id: P2-2
title: Door transitions + Garden area
phase: 2
depends_on: P2-1
status: todo
attempts: 0
acceptance:
 - Door scene/script (Area2D zone) with exported target_area + target_entry strings
 - player body entering a door zone triggers: screen fade out -> AreaManager.load_area(target_area, target_entry) -> player placed at target entry -> fade in
 - fade implemented via a persistent CanvasLayer ColorRect alpha tween (lives in Main/UI so it survives area swaps)
 - new scenes/areas/Garden.tscn, visually distinct from House (different floor color + simple decor)
 - House has a door -> Garden; Garden has a door -> House (bidirectional, each lands at the correct entry marker)
 - current_area is saved on every transition
 - tests/test_doors.gd: Door resolves target_area/target_entry; AreaManager transition switches current area + updates saved current_area (headless, logic-level; no real fade timing)
files: [scripts/Door.gd, scenes/Door.tscn, scenes/areas/Garden.tscn, scripts/AreaManager.gd, scripts/Main.gd, tests/test_doors.gd]
notes: transition is "walk into door zone" (not menu). Keep fade short (~0.3s). Reuse the AreaManager from P2-1. Keep files <500 lines.
