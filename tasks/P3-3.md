id: P3-3
title: Big city map v1 (replaces tiny Town)
phase: 3
depends_on: P3-2, P3-1
status: done
attempts: 0
acceptance:
 - scenes/areas/City.tscn + scripts/CityMap.gd holding an ASCII layout of at least 100x120 cells
   (>= 1600x1920 px world) built through MapBuilder
 - city has visible districts: central plaza, market street, residential block, park, river with
   at least one bridge — buildings and water block movement, roads/bridge walkable
 - AreaManager maps area key "town" -> City.tscn (existing saves with current_area "town" load the
   city seamlessly; no save migration needed)
 - Garden <-> City doors work bidirectionally with correct entry markers
 - camera clamps to city bounds (get_area_bounds from MapBuilder)
 - tests: layout parses to expected size, entries exist, Garden<->City door round trip headless
files: [scenes/areas/City.tscn, scripts/CityMap.gd, scripts/AreaManager.gd, scenes/areas/Garden.tscn, tests/test_city.gd]
notes: layout string lives in CityMap.gd as const (keep the file <500 lines — layout rows are
  one string each, fine). Old Town.tscn deleted, its NPC moves to city in P3-5 (keep one NPC now
  so existing NPC test passes, or update that test). Wire Door scenes at 'D' cells with a small
  door table in CityMap.gd: cell -> {target_area, target_entry}.
