id: P3-1
title: Zoomed-out camera + bounds clamp
phase: 3
depends_on: none
status: todo
attempts: 0
acceptance:
 - default camera zoom is 2.0 (was 3.5) — noticeably more world visible
 - camera limits clamp to the current area's bounds so void is never visible at edges
 - pure static function (e.g. CameraRig.compute_limits(bounds: Rect2) -> Dictionary) unit-tested
 - area may optionally override zoom via an exported property (interiors can stay closer)
 - all existing tests still pass
files: [scripts/AreaManager.gd, scripts/CameraRig.gd, tests/test_camera.gd]
notes: camera is created in AreaManager._place_player(); extract into scripts/CameraRig.gd
  (static helpers + setup). Area bounds source = area node method get_area_bounds() -> Rect2
  if present, else fallback no limits. House/Garden/Town get bounds matching their walls.
  Keep smoothing on. Do NOT change viewport size (540x960 portrait stays).
