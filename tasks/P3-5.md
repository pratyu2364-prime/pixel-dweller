id: P3-5
title: Populate city — NPCs + district labels
phase: 3
depends_on: P3-3
status: done
attempts: 0
acceptance:
 - at least 5 NPCs placed across districts, each with a distinct greeting (reuse Npc.tscn,
   cooldown mood boost unchanged)
 - district name label: as player crosses district regions (rect zones defined in CityMap.gd)
   the on-screen area label updates (e.g. "Market Street", "Riverside Park")
 - pure function district_at(pos: Vector2) -> String unit-tested
 - tests: NPC count/greetings, district_at boundaries
files: [scripts/CityMap.gd, scenes/areas/City.tscn, scripts/UI.gd, tests/test_districts.gd]
notes: reuse the existing area-name label from P2-6 — feed it district names inside the city.
  Greetings: cozy, kid-friendly tone (game is for owner's sister).
files_hint: district zones = Array[Dictionary] {name, rect} in CityMap.gd.
