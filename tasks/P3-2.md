id: P3-2
title: MapBuilder — ASCII layout -> world (LEAD-WRITTEN CORE)
phase: 3
depends_on: none
status: done
attempts: 0
acceptance:
 - scripts/MapBuilder.gd builds a world from an ASCII layout string (each char = 16px cell)
 - pure static parse(layout: String) -> Dictionary {size: Vector2i, cells, entries, doors} unit-tested
 - supported chars at minimum — '.' grass, ',' grass alt, '#' building/wall (solid), 'r' road,
   'p' plaza/paving, 'w' water (solid), 't' tree (solid), 'f' flowers, 'b' bridge (walkable over water),
   digits 1-9 entry markers, 'D' door cell
 - renders via a single TileMap(Layer) using a runtime-generated solid-color TileSet atlas
   (Image.create per tile color, 16x16) — no binary art assets required
 - solid cells get collision (TileSet physics layer OR merged StaticBody2D rects — either, tested)
 - build(layout, parent) creates the TileMap, collisions, and Marker2D nodes named Entry<digit>
 - a small demo layout is exercised in tests headless (bounds, walkable vs solid, entry positions)
files: [scripts/MapBuilder.gd, tests/test_map_builder.gd]
notes: THE core of Phase 3 — lead (Claude) writes this file directly, workers build on top.
  Keep MapBuilder <500 lines. Door cells ('D') only record positions in parse(); actual Door
  scene wiring happens in P3-3 (needs target_area metadata). Colors: cozy pastel palette.
  get_area_bounds() -> Rect2 exposed for P3-1 camera clamp.
