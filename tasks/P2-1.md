id: P2-1
title: Area framework + dynamic area loading
phase: 2
depends_on: none
status: done
attempts: 0
acceptance:
 - areas are swappable scenes loaded at runtime; UI + Dweller persist across loads
 - current World content becomes scenes/areas/House.tscn (no behavior change for House)
 - new AreaManager (scripts/AreaManager.gd) exposes load_area(area_name, entry_name): instantiates the area scene into a Main area-container node, places player at the area's named entry Marker2D, attaches the camera to the player
 - UI no longer hardcodes get_node("../World"); it routes apply_world_stage through AreaManager to the current area (no-op if the area lacks the method)
 - SaveManager round-trips a current_area field (default "house"); on boot, AreaManager loads the saved area at its default entry
 - all existing tests stay green; tests/test_area_manager.gd: load_area sets current area + places player at entry; save/load round-trips current_area
files: [scripts/Main.gd, scripts/AreaManager.gd, scenes/Main.tscn, scenes/areas/House.tscn, scripts/UI.gd, scripts/SaveManager.gd, tests/test_area_manager.gd]
notes: foundation only — House is the single area, loaded through the new framework. NO new areas/doors yet (those are P2-2+). Player is re-instantiated per area at the entry marker; Dweller state lives in UI (persistent), separate from the player node. Keep files <500 lines.
